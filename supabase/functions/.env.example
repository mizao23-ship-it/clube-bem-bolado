# ── Supabase Edge Functions — variáveis locais ────────────────────────────────
# Copie para supabase/functions/.env e preencha para rodar localmente.
# Em produção, use: supabase secrets set CHAVE=valor

# New Value API
NV_TOKEN_RH=placeholder_substituir_depois
NV_UUID_RH=placeholder_substituir_depois

# Preenchidos automaticamente pelo Supabase em produção:
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
